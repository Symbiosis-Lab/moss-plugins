coverage:
  status:
    project:
      default:
        target: 70%
    patch:
      default:
        target: 80%

flags:
  github:
    paths:
      - github/
    carryforward: true
  matters:
    paths:
      - matters/
    carryforward: true
  email:
    paths:
      - email/
    carryforward: true
  # Add more plugins as they're added to the repo
