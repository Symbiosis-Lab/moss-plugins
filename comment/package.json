{
  "name": "@symbiosis-lab/moss-plugin-comment",
  "version": "0.1.0",
  "description": "Comment section plugin for Moss - renders stored comments and accepts new ones",
  "main": "dist/main.bundle.js",
  "scripts": {
    "build": "npm run clean && npm run bundle && npm run copy-assets",
    "bundle": "esbuild src/main.ts --bundle --format=iife --global-name=CommentPlugin --external:@symbiosis-lab/moss-api --outfile=dist/main.bundle.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "copy-assets": "cp assets/* dist/ && cp browser/moss-comments.css dist/"
  },
  "keywords": [
    "moss",
    "plugin",
    "comment",
    "social"
  ],
  "author": "Symbiosis Lab",
  "license": "MIT",
  "devDependencies": {
    "@symbiosis-lab/moss-api": "^0.5.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.3.0"
  }
}
