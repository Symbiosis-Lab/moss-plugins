{
  "name": "@symbiosis-lab/moss-plugin-review",
  "version": "0.1.0",
  "description": "Enrich review posts with metadata from NeoDB",
  "main": "dist/main.bundle.js",
  "scripts": {
    "build": "npm run clean && npm run bundle && npm run copy-assets",
    "dev": "npm run clean && npm run copy-assets && concurrently \"npm run bundle -- --watch\" \"npm run watch-assets\"",
    "bundle": "esbuild src/main.ts --bundle --format=iife --global-name=ReviewPlugin --outfile=dist/main.bundle.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "copy-assets": "cp assets/* dist/ && cp browser/review.css dist/",
    "watch-assets": "chokidar 'assets/**/*' 'browser/**/*' -c 'npm run copy-assets'",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "author": "Symbiosis Lab",
  "license": "MIT",
  "devDependencies": {
    "@symbiosis-lab/moss-api": "workspace:*",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.2.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
